hl.config({
    decoration = {
        rounding       = 20,
        rounding_power = 2,

        shadow = { enabled = false },

        blur = {
            enabled = false,
            -- size    = 8,
            -- passes  = 3,
        },
    }
})
